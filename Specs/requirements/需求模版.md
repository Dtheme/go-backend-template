# {version} 需求

> 复制本文件到 `Specs/requirements/{version}/需求.md` 后填写。本目录由人工维护，AI 只读。
> 每个功能都要写清「做什么」「接口与数据」「业务规则与边界」「验收标准」，AI 据此出技术方案；写不清的地方 AI 会先提问，不会自行假设。
> 功能编号固定为 `F-{version}-NNN`（三位数字），验收标准编号为 `AC-n`；技术方案的需求摘要与测试计划用它们追溯，`make check` 中的 `spec-check` 会核对每个功能编号都出现在技术方案里。

## 版本信息

- 版本号：{version}（语义化三段，如 `0.1.0`；本文件所在目录名必须与版本号一致）
- 目标日期：
- 负责人：

## 背景与目标

<!-- 为什么做这个版本、解决什么问题、成功标准是什么 -->

## 功能列表

### F-{version}-001 {功能名称}

**做什么**

<!-- 一段话说明功能目标与使用者 -->

**接口与数据**

| 方法 | 路径 | 用途 | 鉴权 |
| --- | --- | --- | --- |
| POST | /v1/xxx | | |

请求：

```json
{}
```

响应（成功）：

```json
{}
```

错误情况：

| 场景 | HTTP 状态码 | error.code |
| --- | --- | --- |
| 参数缺失 | 400 | invalid_argument |

**业务规则与边界**

<!-- 校验规则、幂等要求、并发/重复提交、数据唯一性、空值与极限值 -->

**验收标准**

<!-- 可直接转成测试用例的断言，逐条列出 -->

- [ ] AC-1 
- [ ] AC-2 

### F-{version}-002 {功能名称}

<!-- 同上 -->

## 数据变更

<!-- 新增/修改的表、字段、索引、唯一约束；无则写「无」 -->

## 非功能需求

- 性能：<!-- 例如 p99 < 200ms，QPS 预期 -->
- 安全：<!-- 鉴权、敏感字段、限流 -->
- 兼容：<!-- 对已有接口是否有破坏性变更 -->

## 验证方式

<!-- 用户如何验收：Postman 集合 / curl；需要的测试数据或前置条件 -->

## 变更记录

| 日期 | 变更内容 | 备注 |
| --- | --- | --- |
